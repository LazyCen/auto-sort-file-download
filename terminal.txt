Step 1
1. Open your terminal (Ctrl + Alt + T)

2. Install the monitoring tool by running:
     sudo apt update && sudo apt install inotify-tools -y


3. Create a background automation script:
     nano ~/auto_sort.sh

4. Open script.txt file. Copy and paste the following script into the window:

5. Save and close out of nano (Ctrl + O, Enter, then Ctrl + X)

Step 2
1. Open your terminal (Ctrl + Alt + T).

2. Run this command to completely stop any hidden or broken versions of the script running in the background:
     pkill -f auto_sort.sh

3. Start the script to test it live
     /bin/bash ~/auto_sort.sh

Step 3 
Test It While the Terminal is Open
1. Go to your browser
2. Download any file, video, or images
3. Check your folder

Once verified, press Ctrl + C in the terminal and push it to the background permanently using:
     nohup ~/auto_sort.sh > /dev/null 2>&1 &
